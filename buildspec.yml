version: 0.2

phases:
  pre_build:
    commands:
      - npm install -g yarn
      - yarn
  build:
    commands:
      - yarn build

artifacts:
  files:
    - '**/**'

cache:
  paths:
    - 'usr/local/bin/yarn/**/*'